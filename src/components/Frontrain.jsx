import React from 'react'
import styled from 'styled-components'

const Frontrain = styled.span`
  font-family: 'Press Start 2P', monospace;
`

export default (props) => <Frontrain {...props}>frontrain</Frontrain>
